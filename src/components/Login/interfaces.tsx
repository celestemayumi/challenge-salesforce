// 'use client';
export default interface User {
  email: string;
  empresa: string;
  nome: string;
  senha: string;
  telefone: string;
}
export default interface Login {
  email: string;
  senha: string;
}
